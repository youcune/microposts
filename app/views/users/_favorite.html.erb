<% if current_user.favorite?(favorite) %>
  <%= link_to "unfav", unfavorites_micropost_path(favorite),
    remote: true, class: "btn btn-warning btn-xs", id: "favorite-#{favorite.id}", method: :post %>
<% else %>
　<%= link_to "fav", favorites_micropost_path(favorite),
    remote: true, class: "btn btn-default btn-xs", id: "favorite-#{favorite.id}", method: :post %>
<% end %>
